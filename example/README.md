# serverless-rolldown example

This example validates the local `serverless-rolldown` plugin against an
official Serverless Framework v4 scaffold.

## Commands

```bash
bun install
bun run package
bun run invoke:hello
bun run validate
```
